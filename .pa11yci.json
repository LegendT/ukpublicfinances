{
  "defaults": {
    "standard": "WCAG2AA",
    "timeout": 30000,
    "wait": 1000,
    "chromeLaunchConfig": {
      "args": [
        "--no-sandbox",
        "--disable-setuid-sandbox"
      ]
    }
  },
  "urls": [
    "http://127.0.0.1:8081/404.html"
  ],
  "_note": "Routes come from the sitemap at run time (see the a11y:ci script), so every page the site builds is audited and no list needs maintaining. 404.html is listed here because it is deliberately absent from the sitemap."
}